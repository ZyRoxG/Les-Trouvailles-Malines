# Le renvoi vers la page d'accueil pour les adresses comme /a-propos est
# désormais géré par le fichier wrangler.jsonc (réglage "not_found_handling":
# "single-page-application"), la méthode officielle recommandée par
# Cloudflare. Ce fichier n'a plus besoin de règle de renvoi.